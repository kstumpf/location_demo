{% extends "base.html" %}

{% block content %}
  <h1>Create a new Location</h1>
  
  <p>
  <form action="{% url 'locations:create' %}" method="post">{% csrf_token %}
    <ul>
<!--      {{ form.as_ul }} -->
      <li>Location Name: {{ form.name }}
      <li>Location Type: {{ form.type }}
      <li>Location Parent: {{ form.parent }}
      <li>Locations that Overlap: {{ form.overlapswith }}
    </ul>

    <input type="submit" value="Submit" />
  </form></p>
{% endblock %}
